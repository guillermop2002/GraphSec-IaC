name: 'GraphSec-IaC Análisis de Seguridad'

on:
  pull_request:
    branches: [ main ]
  workflow_dispatch:  # Permite ejecución manual desde GitHub Actions
  push:
    branches: [ main ]  # Ejecutar también cuando se hace push a main

jobs:
  analysis:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write
      pull-requests: write
    
    steps:
      # 1. Checkout del código del PR
      - name: Checkout del código
        uses: actions/checkout@v4
      
      # 2. Configurar Python
      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      
      # 3. Configurar Terraform
      - name: Configurar Terraform
        uses: hashicorp/setup-terraform@v3
      
      # 4. Instalar Trivy (via apt-get)
      - name: Instalar Trivy
        run: |
          sudo apt-get install -y wget apt-transport-https gnupg
          wget -qO - https://aquasecurity.github.io/trivy-repo/deb/public.key | sudo apt-key add -
          echo "deb https://aquasecurity.github.io/trivy-repo/deb $(lsb_release -sc) main" | sudo tee -a /etc/apt/sources.list.d/trivy.list
          sudo apt-get update
          sudo apt-get install -y trivy
      
      # 5. Instalar dependencias de Python (Checkov, etc.)
      - name: Instalar dependencias de Python
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          # Forzar instalación de checkov explícitamente
          python -m pip install checkov
          # Verificar que checkov está instalado y disponible
          which checkov || echo "Checkov no encontrado en PATH"
          checkov --version || echo "Checkov no se puede ejecutar"
      
      # 6. Clonar el repositorio de VPC (para prueba)
      - name: Clonar el repositorio de VPC (para prueba)
        run: |
          git clone https://github.com/terraform-aws-modules/terraform-aws-vpc.git --depth 1
      
      # 6.5. Cachear resultados del pipeline (grafo y escáneres)
      - name: Restaurar caché del pipeline
        id: cache-pipeline
        uses: actions/cache@v3
        with:
          path: .graphsec_cache
          key: pipeline-cache-${{ runner.os }}-${{ hashFiles('terraform-aws-vpc/**/*.tf') }}-v22.5
          restore-keys: |
            pipeline-cache-${{ runner.os }}-
      
      # 6.6. Verificar contenido del caché restaurado
      - name: Verificar caché restaurado
        run: |
          echo "=== Caché restaurado: ${{ steps.cache-pipeline.outputs.cache-hit }} ==="
          if [ -d .graphsec_cache ]; then
            echo "=== Directorios de Checkov en caché ==="
            find .graphsec_cache -type d -name "checkov_vpc_*" || echo "No se encontraron directorios de Checkov"
            echo ""
            echo "=== Archivos de Trivy en caché ==="
            find .graphsec_cache -type f -name "trivy_vpc_*" | head -5 || echo "No se encontraron archivos de Trivy"
          else
            echo "Directorio .graphsec_cache no existe después de restaurar"
          fi
      
      # 7. Inicializar Terraform (analizando 'terraform-aws-vpc')
      - name: Terraform Init
        run: terraform init -backend=false
        working-directory: ./terraform-aws-vpc
      
      # 7.1. Verificar que terraform-aws-modules existe después de init
      - name: Verificar directorio terraform-aws-modules
        run: |
          echo "Verificando estructura de directorios después de terraform init:"
          ls -la ./terraform-aws-vpc/ | head -20
          echo ""
          echo "Buscando directorio terraform-aws-modules:"
          if [ -d "./terraform-aws-vpc/terraform-aws-modules" ]; then
            echo "✅ Directorio terraform-aws-modules/ EXISTE"
            find ./terraform-aws-vpc/terraform-aws-modules -name "*.tf" | head -10
          else
            echo "❌ Directorio terraform-aws-modules/ NO EXISTE"
            echo "Buscando en .terraform/modules:"
            if [ -d "./terraform-aws-vpc/.terraform/modules" ]; then
              echo "✅ Directorio .terraform/modules/ EXISTE"
              find ./terraform-aws-vpc/.terraform/modules -name "*.tf" | head -10
            else
              echo "❌ Directorio .terraform/modules/ TAMPOCO EXISTE"
            fi
          fi
      
      # 8. Ejecutar el Pipeline (analizando 'terraform-aws-vpc')
      # PR #31: Prueba de análisis VPC
      - name: Ejecutar Pipeline de GraphSec-IaC
        id: run_pipeline
        run: |
          PR_NUMBER="${{ github.event.pull_request.number || github.run_number }}"
          echo "PR_NUMBER=${PR_NUMBER}" >> $GITHUB_ENV
          # Usar nombre fijo para el caché ('vpc'), pero el output JSON tendrá el PR number
          python run_pipeline.py \
            --directory ./terraform-aws-vpc \
            --project vpc \
            --output graph_data.json
      
      # 8.5. Actualizar metadata del JSON con el PR number
      - name: Actualizar metadata del JSON
        run: |
          PR_NUMBER="${{ github.event.pull_request.number || github.run_number }}"
          python -c "
          import json
          import sys
          pr_num = '${{ github.event.pull_request.number || github.run_number }}'
          with open('graph_data.json', 'r') as f:
              data = json.load(f)
          metadata = data.get('api_metadata', {})
          metadata['project_name'] = f'vpc_pr_{pr_num}'
          metadata['pr_number'] = int(pr_num)
          data['api_metadata'] = metadata
          with open('graph_data.json', 'w') as f:
              json.dump(data, f, indent=2)
          "
      
      # 8.6. Verificar contenido del caché antes de guardar
      - name: Verificar contenido del caché
        run: |
          echo "=== Contenido de .graphsec_cache antes de guardar ==="
          if [ -d .graphsec_cache ]; then
            find .graphsec_cache -type f -o -type d | head -20
            echo ""
            echo "=== Directorios de Checkov ==="
            find .graphsec_cache -type d -name "checkov_vpc_*" || echo "No se encontraron directorios de Checkov"
            echo ""
            echo "=== Archivos de Trivy ==="
            find .graphsec_cache -type f -name "trivy_vpc_*" | head -5 || echo "No se encontraron archivos de Trivy"
          else
            echo "Directorio .graphsec_cache no existe"
          fi
      
      # 8.7. Guardar caché del pipeline
      - name: Guardar caché del pipeline
        uses: actions/cache@v3
        with:
          path: .graphsec_cache
          key: pipeline-cache-${{ runner.os }}-${{ hashFiles('terraform-aws-vpc/**/*.tf') }}-v22.5
      
      # 9. Renombrar artefacto JSON
      - name: Renombrar artefacto JSON
        run: |
          PR_NUMBER="${{ github.event.pull_request.number || github.run_number }}"
          mv graph_data.json pr-${PR_NUMBER}.json
          echo "PR_NUMBER=${PR_NUMBER}" >> $GITHUB_ENV
      
      # 10. Copiar index.html a la raíz para GitHub Pages
      - name: Copiar index.html a la raíz
        run: cp static/index.html index.html
      
      # 11. Preparar directorio de despliegue
      - name: Preparar directorio de despliegue
        run: mkdir deploy_dir
      
      # 12. Mover artefactos al directorio de despliegue
      - name: Mover artefactos al directorio de despliegue
        run: |
          mv pr-${PR_NUMBER}.json ./deploy_dir/
          mv index.html ./deploy_dir/
      
      # 13. Desplegar informe en GitHub Pages
      - name: Desplegar informe en GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: deploy_dir
      
      # 14. Publicar un comentario en el PR (solo si hay PR)
      - name: Publicar Comentario en PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            // Cargar el JSON generado
            const fs = require('fs');
            const prNumber = context.issue.number;
            const results = JSON.parse(fs.readFileSync(`deploy_dir/pr-${prNumber}.json`, 'utf8'));
            
            // Usar valores por defecto ({})
            const metadata = results.api_metadata || {};
            const correlation = metadata.correlation_metadata || {};
            
            // Extraer métricas
            const nodes_vulnerable = correlation.nodes_with_issues_count || 0;
            const findings_unique = metadata.total_findings_unique || 0;
            const nodes_total = metadata.total_nodes || 0;
            const findings_unassigned = metadata.unassigned_findings_count || 0;
            
            // Construir la URL del informe visual
            const report_url = `https://guillermop2002.github.io/GraphSec-IaC/?report=https://guillermop2002.github.io/GraphSec-IaC/pr-${prNumber}.json`;
            
            // Construir el comentario
            const body = `
            ## 🔬 Análisis de GraphSec-IaC Completado
            
            Se han analizado los cambios en la infraestructura.
            
            | Métrica | Resultado |
            | :--- | :--- |
            | Nodos Vulnerables | **${nodes_vulnerable}** |
            | Hallazgos Únicos | **${findings_unique}** |
            | Nodos Totales | ${nodes_total} |
            | Hallazgos No Asignados | ${findings_unassigned} |
            
            [**🔗 Ver Informe Visual Interactivo**](${report_url})
            `;
            
            // Publicar el comentario
            github.rest.issues.createComment({
              issue_number: prNumber,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: body
            });


